Resolver and VO notes land here.

- `daniel-voice.json` — written by `scripts/resolve_voice.py` (the verified `voice_id` when one exists)
- `waiting-on-read-vo.json` — written after a full eleven_v3 render

These files are the handoff for the Unmoved agent. Do not put API keys in them.
